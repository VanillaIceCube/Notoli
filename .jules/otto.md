# Otto's Backlog Organization Journal

## 2026-09-09 - Direct GitHub REST API Backlog Audit **Learning:** In sandboxed environments where GitHub CLI `gh` is unavailable, querying `https://api.github.com/repos/VanillaIceCube/Notoli/issues` and `/pulls` using standard HTTP clients (e.g., Python `urllib.request`) provides complete visibility over open issues, PRs, labels, and state without requiring external dependencies. **Action:** Perform future daily backlog audits by querying GitHub REST API endpoints directly when CLI tools are not installed.

## 2026-09-09 - Protecting Active Specialist Implementation **Learning:** Active implementation PRs (such as Nora's #790 addressing #511, Marty's backend test suite acceleration PRs addressing #563, Kira's #767, and Forge's #744) represent work in flight. Re-scoping, splitting, or closing issues while implementation PRs are open creates goalpost movement for implementation agents. **Action:** Treat issues with open specialist implementation PRs as active work, preserving their scope, acceptance criteria, and status during daily backlog audits.

## 2026-09-09 - Automated Security Alert Issues **Learning:** Security alert issues (prefixed with `<!-- notoli-security-alert:...`) are automatically synchronized and managed by scheduled workflows (`alert-codeql.yml`, `alert-vulnerability.yml`, `alert-malware.yml`). Manual edits to titles or descriptions can disrupt automated tracking. **Action:** Leave security alert issues to automated workflow reconciliation and focus manual backlog organization on product, engineering, and architecture tickets.

## 2026-09-09 - Upstream FullStackTemplate Adoption Issues **Learning:** Issues #668-#676 form a structured cross-repository parity adoption epic roadmap for Notoli. Although some adoption tasks overlap with older standalone feature requests (such as #88 for Google login and #582 for email verification), the adoption tickets represent explicit parity objectives to be implemented by specialist agents. **Action:** Preserve the scope, boundaries, and structure of upstream adoption tickets while noting cross-links and relationships to existing feature requests.
